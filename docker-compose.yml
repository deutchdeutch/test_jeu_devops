version: '3.8'

services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"

  two-ships:
    build:
      context: ./jeu_1_two_ships_passing_the_night
      dockerfile: Dockerfile
    ports:
      - "8081:80"

  space-invaders:
    build:
      context: ./jeu_2_Space_Invaders
      dockerfile: Dockerfile
    ports:
      - "8082:80"